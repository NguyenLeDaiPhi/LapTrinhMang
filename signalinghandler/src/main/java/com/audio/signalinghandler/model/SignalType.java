package com.audio.signalinghandler.model;

public enum SignalType {
    OFFER,
    ANSWER,
    ICE,
    JOIN,
    LEAVE,
    KEY_EXCHANGE,
    ENCRYPTION_ENABLED,
    REQUEST_USERS,
    USER_LIST,
    CALL_REQUEST,
    CALL_ACCEPTED,
    CALL_REJECTED,
    CALL_ENDED
}
